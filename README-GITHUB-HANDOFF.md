Apply from project root:

```bash
cd /Users/hazel/AndroidStudioProjects/NoirPlayboxOperatorStarterV1
unzip -o ~/Downloads/noir-playbox-android-github-handoff-v1.zip -d .
./scripts/github-preflight.sh
```

Then review `git status` before committing.
